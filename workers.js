addEventListener('fetch', event => {  event.respondWith(handleRequest(event.request));});async function handleRequest(request) {  if (request.method === 'POST') {      const { token } = await request.json();      const secretKey = 'Your_Secret_key_here';       const response = await fetch(`https://recaptcha.net/recaptcha/api/siteverify?secret=${secretKey}&response=${token}`, {          method: 'POST'      });      const data = await response.json();      // 设置 CORS 头      const headers = new Headers({          'Content-Type': 'application/json',          'Access-Control-Allow-Origin': '*', // 允许的源          'Access-Control-Allow-Methods': 'POST, OPTIONS', // 允许的请求方法          'Access-Control-Allow-Headers': 'Content-Type' // 允许的请求头      });      return new Response(JSON.stringify({ score: data.score }), {          headers: headers      });  }  // 处理 OPTIONS 请求（预检请求）  if (request.method === 'OPTIONS') {      return new Response(null, {          headers: {              'Access-Control-Allow-Origin': '*',              'Access-Control-Allow-Methods': 'POST, OPTIONS',              'Access-Control-Allow-Headers': 'Content-Type'          }      });  }  return new Response('Error:Method Not Allowed', { status: 405 });}